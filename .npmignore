# Source — published as compiled dist/ only
src/
tests/
scripts/
test-site/

# Tooling
.idea/
.vscode/
.github/
*.tsbuildinfo
tsconfig.json
vitest.config.ts
pnpm-lock.yaml
pnpm-workspace.yaml
.gitignore

# Installer scripts — fetched via curl/iwr, not shipped in the npm tarball
install.sh
install.ps1

# Runtime / scratch
*.log
*.swp
*.swo
.DS_Store
coverage/
.tmp/
temp/

# Only ship what package.json "files" lists:
#   dist/, bin/, README.md, LICENSE
